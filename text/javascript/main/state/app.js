import {same} from '../f/general/i.js';

export default (
    () => {
        return {
            id_lang: 3762095517704366129n,
            last_key:"",
            
            value_blured: same(""),

            separator: same("\n"),
            separator_block: same("\n\nbetween blocks\n\n"),
            separator_row: same("\n"),
        };
    }
)();
