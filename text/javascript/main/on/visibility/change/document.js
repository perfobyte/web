

export default (
    (e) => {
        var
            document = e.currentTarget,

            state = document.visibilityState
        ;
        
        if (state === "visible") {
                
        }
        else {
            
        }
    }
)