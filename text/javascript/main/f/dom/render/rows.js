
export default (
    (
        lines,
        
        i,
        l,

        to,
        from,
        
        dom,
        dom_i,

        content,
    ) => {
        while (i < l) {
            dom_elements[dom_i++]
            .firstElementChild
            .textContent = (
                content[
                    lines[i++]
                ]
                .substring(
                    (lines[i++]),
                    (lines[i++]),
                )
            );
        };

        return dom_elements;
    }
);
