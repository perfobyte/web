import {msg_to_html, msg_time_to_html, count_char, date, time, msg_time_format, detail_time, msg_height} from '../i.js';

export default (
    (e) => {
        var
            src = a.v
        ;
        return (
            msg_to_html(
                e.value,
                e.id,
                e.rows,
                list,
                MESSAGE_EL.cloneNode(true),
                0,
            ),

            r
        );
    }
);
