
*AI;

extension [
    for website,
    for repository view,
    for zip view,
],

feature="analyze large programs in a short time" [
    safety,
    performance,
    size,
    errors,
],

"function" = bad_code_detection(html,css,js),

local(in([perfobyte_os]))=free,
remote=subscription;
