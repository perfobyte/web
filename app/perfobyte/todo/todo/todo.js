
rewrite into NodeJS version;

contextmenu c0;
c1 [
    minp sticky,
]

encrypt all app data;
open db;
image,video,audio message;

chat add;
message send;

create_link_form
+
("./link_action.js");

export;
import;



Internet [
    module.local,
    "./login.js",

    module.regular,
    module.email,

    optimize_app_sources,
];

program [
    module.bot [
        perfobyte (administrator) (AI),
    ],
];

search chats by name;
settings on;

adapt mobile;
visuability;

tag(1);


LAN [
    module.wifi_router,
    module.bluetooth_low_energy,
];

protection [
    module.secure,
    module.email_confidential,
    module.email_protected,
];

VoIP [
    module.voip,
];

finance [
    module.bitcoin_remote_access,
];

open(LLC);
native([perfobyte_js]),

Local network [
    module.bluetooth_two_way,

    module.wifi_direct,
    module.wifi_ibss,
    module.wifi_aware,
    module.wifi_apple_mc,
];

program [
    ssh,
];
