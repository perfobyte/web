
[
    APP module;
    perfobyte chat as console;
];
Search Engine (cache, semantics of webapplication by url) && (Free search by URL);
detailed hardware and software settings in perfobyte;

SIM module;
CELL NET call module;
tec app control and compilation;

foundation of OS are (app and extensions);


perfobyte spell (
    backward compatibility of versions
    + (
        Two-way compatibility between the web and zedrit standards versions
    )
);
