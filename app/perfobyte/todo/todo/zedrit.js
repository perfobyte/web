
under_tm(zedrit) [

    app.tec = [
        code_analyzer [
            detect and control (delete,paste,write,add) specific pieses of code,
        ];

        (() => {
            return requirements;
        })();
    ];

    app.clear = [
        * (uncensored_logic);

        * fulfills according to requirements;

        ((requirements) => {
            return (
                [
                    accept,
                    generate
                ] [
                    text;
                    code [
                        html,
                        css,
                        js,
                    ];
                    audio;
                    video;
                ]
            );
        })(requirements);
    ];
    
    import './os/i.js';
    zedrit phone;
    zedrit;

    zedrit pc;
    zedrit tablet;
    zedrit laptop;
];