import {bit_get} from '../../f/i.js';

import {cl_u_cl} from '../../../elems.js';


export default (
    function(e) {
        // var
        //     d = new Uint8Array(e.data)[0]
        // ;

        return (
            undefined
            // (bit_get(d,0) === 0)
            // &&
            // (bit_get(d,1) === 1)
            // &&
            // (
            //     cl_u_cl[
            //         (bit_get(d,2) === 1) && (bit_get(d,3) === 1)
            //         ? "add"
            //         : "remove"
            //     ]("a")
            // )
        );
    }
);
